const config = {
  HOST: 'localhost',
  PORT: 8000,

  env: '', // default

  // Environments
  dev: 'development',
  testing: 'testing',
  staging: 'staging',
  prod: 'production'
};

module.exports = config;
