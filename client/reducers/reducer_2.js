// Import actions
import {  } from '../actions/index';


export default function EnglishCurrentReducer(state=0, action) {
  const current = state;
  switch(action.type){
    
    default:
      return state;
  }
}